# =============================================================================
# docker-compose.yml for cn-rail-monitor
# =============================================================================
# Usage:
#   docker-compose up -d          # Start in background
#   docker-compose logs -f        # View logs
#   docker-compose down           # Stop and remove
# =============================================================================

services:
  cn-rail-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cn-rail-monitor
    restart: unless-stopped
    
    ports:
      - "8080:8080"
    
    volumes:
      # Mount configuration file (read-only for safety)
      - ./config.yaml:/app/config.yaml:ro
      # Optional: mount log directory
      - ./logs:/app/logs
    
    environment:
      - CONFIG_PATH=/app/config.yaml
    
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    
    # Optional: limit resources
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.5'
    #       memory: 256M

  # Optional: Prometheus to scrape metrics
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #   depends_on:
  #     - cn-rail-monitor

  # Optional: Grafana to visualize metrics
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: grafana
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   depends_on:
  #     - prometheus
